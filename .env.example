# Root pointer: canonical dev templates are:
#   - env/.env.example       — порты как в docker-compose (Postgres 5433, Redis 6380)
#   - backend/.env.example   — копия в backend/.env для uvicorn без Docker
# Production: .env.production.example
