# Committed defaults for `docker compose` (env_file must exist).
# Override with backend/.env locally (gitignored) if needed.

ENVIRONMENT=local
SECRET_KEY=dev-secret-change-me

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/enterprise_copilot
REDIS_URL=redis://localhost:6379/0

STORAGE_BACKEND=local
INGESTION_ASYNC_ENABLED=0

LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
