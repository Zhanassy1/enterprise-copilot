# Environment templates

- **`.env.example`** — локальная разработка (порты как в `docker-compose.yml`: Postgres `5433`, Redis `6380`). Скопируйте в `backend/.env` и поправьте значения.
- **`../.env.production.example`** — продакшен / staging (корень репозитория).
